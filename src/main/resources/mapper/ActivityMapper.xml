<mapper>
    <select id="selectByMiniProgramId" resultType="org.hxj.entity.po.Activity">
        select * from activity where id in (
        <for item="id" collection="ids">
            #{id.id}
        </for>
        )
    </select>
</mapper>